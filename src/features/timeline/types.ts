import { Path } from "../../navigation/types";

type Timeline = {
  title: string;
  year: string;
  image: string;
  alt: string;
  shortDescription: string;
  pathTo: Path;
};

export type { Timeline };
